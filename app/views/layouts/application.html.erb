<!DOCTYPE html>
<html>
<head>
  <title>Web App</title>
  <%= stylesheet_link_tag "web-app-theme/base", "web-app-theme/themes/default/style", "web-app-theme/override", "custom" %>
  <%= csrf_meta_tag %>
</head>
<body>
  <div id="container">
    <div id="header">
      <h1><a href="/">Web App</a></h1>
      <div id="user-navigation">
        <ul class="wat-cf">
            <!--<li >Bienvenue <%= current_user.email %></li>-->
            <% if current_user.company.nil? %>
              <li><%= link_to t('assur.general.Configuration'), new_company_path %></li>
            <% else %>
              <li><%= link_to t('assur.general.Configuration'), edit_company_path(current_user.company) %></li>
            <% end %>
            <li><%= link_to t("assur.general.logout"), destroy_user_session_path, :class => 'logout' %></li>         
        </ul>
      </div>
      <%= render :partial => 'shared/main_navigation' %>
      <div id="main-navigation">
        <ul class="wat-cf"></ul>
      </div>
    </div>    
    <div id="wrapper" class="wat-cf">
      <div class="flash">
        <% flash.each do |type, message| -%>
          <div class="message <%= type %>">
            <p><%= message %></p>
          </div>
        <% end -%>
      </div>
      <div id="main">
        <%= yield %>
        <div id="footer">
          <div class="block">
            <p>Copyright &copy; <%= Time.now.year %> Web App.</p>
          </div>      
        </div>
      </div>
      <div id="sidebar">
        <%= yield :sidebar %>
      </div>
    </div>    
  </div>
  <%= javascript_include_tag 'prototype', 'application' %>
  <%= javascript_include_tag 'quick-search' %>
  <%= yield(:javascripts) %>
</body>
</html>